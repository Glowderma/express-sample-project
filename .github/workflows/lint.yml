name: Lint

on:
  pull_request:
    branches:
      - main

jobs:
  lint:
    timeout-minutes: 10
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: npm install

      - name: Run Lintroller
        run: npm run lint
